--- source
+++ source
@@ -485,7 +485,7 @@
     end)
 end
 
-currentVersion = '7.50'
+currentVersion = '7.63'
 BRAND_NAME = 'Infinity Yield Plus'
 
 PANEL_W = 300
@@ -556,6 +556,8 @@
 		keybindToggle = "Create Toggle",
 		keybindToggleOn = "Create Toggle",
 		favorites = "Favorites",
+		favOnly = "Favorites only",
+		favEmpty = "No favorites yet — star a command in the helper or right-click a row",
 		allCommands = "Commands",
 		addFavorite = "Add to favorites",
 		removeFavorite = "Remove from favorites",
@@ -781,6 +783,8 @@
 		keybindToggle = "Создать тоггл",
 		keybindToggleOn = "Создать тоггл",
 		favorites = "Избранное",
+		favOnly = "Только избранное",
+		favEmpty = "Пока пусто — звезда в подсказке или ПКМ по строке",
 		allCommands = "Команды",
 		addFavorite = "В избранное",
 		removeFavorite = "Убрать из избранного",
@@ -1180,7 +1184,11 @@
 	if type(refreshKbCmdPicker) == "function" then
 		refreshKbCmdPicker()
 	end
-	if cmdFavHeader then cmdFavHeader.Text = T("favorites") end
+	if type(updateFavHeader) == "function" then
+		updateFavHeader()
+	elseif cmdFavHeader then
+		cmdFavHeader.Text = T("favorites")
+	end
 	if cmdAllHeader then cmdAllHeader.Text = T("allCommands") end
 	if PopupText_2 then PopupText_2.Text = T("keybindEditorTitle") end
 	if Directions then Directions.Text = T("keybindHint") end
@@ -1717,11 +1725,12 @@
 cmdListLayout.Padding = UDim.new(0, 3)
 cmdListLayout.SortOrder = Enum.SortOrder.LayoutOrder
 
-cmdFavHeader = Instance.new("TextLabel")
+cmdFavHeader = Instance.new("TextButton")
 cmdFavHeader.Name = "IYR_FAV_HDR"
 cmdFavHeader.Parent = CMDsF
 cmdFavHeader.BackgroundTransparency = 1
 cmdFavHeader.BorderSizePixel = 0
+cmdFavHeader.AutoButtonColor = false
 cmdFavHeader.Size = UDim2.new(1, -(SCROLL_GUTTER + 2), 0, 16)
 cmdFavHeader.Font = UI_FONT_MED
 cmdFavHeader.TextSize = 11
@@ -1733,6 +1742,46 @@
 cmdFavHeader.ZIndex = 11
 cmdFavHeader.Visible = false
 cmdFavHeader.LayoutOrder = 1
+cmdFavOnlyFilter = false
+cmdFavEmptyHint = Instance.new("TextLabel")
+cmdFavEmptyHint.Name = "IYR_FAV_EMPTY"
+cmdFavEmptyHint.Parent = CMDsF
+cmdFavEmptyHint.BackgroundTransparency = 1
+cmdFavEmptyHint.BorderSizePixel = 0
+cmdFavEmptyHint.Size = UDim2.new(1, -(SCROLL_GUTTER + 2), 0, 28)
+cmdFavEmptyHint.Font = UI_FONT
+cmdFavEmptyHint.TextSize = 11
+cmdFavEmptyHint.TextXAlignment = Enum.TextXAlignment.Left
+cmdFavEmptyHint.TextYAlignment = Enum.TextYAlignment.Top
+cmdFavEmptyHint.TextColor3 = Color3.fromRGB(154, 148, 176)
+cmdFavEmptyHint.TextWrapped = true
+cmdFavEmptyHint.Text = T("favEmpty")
+cmdFavEmptyHint.TextTruncate = Enum.TextTruncate.AtEnd
+cmdFavEmptyHint.ZIndex = 11
+cmdFavEmptyHint.Visible = false
+cmdFavEmptyHint.LayoutOrder = 2
+function updateFavHeader(favCount)
+	if not cmdFavHeader then return end
+	local n = tonumber(favCount)
+	if n == nil then
+		n = type(favCmds) == "table" and #favCmds or 0
+	end
+	local label = T("favorites")
+	if cmdFavOnlyFilter then
+		label = T("favOnly")
+	end
+	cmdFavHeader.Text = label .. "  " .. tostring(n)
+	cmdFavHeader.TextColor3 = cmdFavOnlyFilter and Color3.fromRGB(255, 232, 160) or Color3.fromRGB(255, 214, 102)
+	if cmdFavEmptyHint then
+		cmdFavEmptyHint.Text = T("favEmpty")
+	end
+end
+cmdFavHeader.MouseButton1Click:Connect(function()
+	cmdFavOnlyFilter = not cmdFavOnlyFilter
+	if type(IndexContents) == "function" and Cmdbar then
+		IndexContents(Cmdbar.Text, true, true)
+	end
+end
 
 cmdAllHeader = Instance.new("TextLabel")
 cmdAllHeader.Name = "IYR_ALL_HDR"
@@ -10873,12 +10922,28 @@
         end)
     end
 
+    if cmdFavOnlyFilter and not takesPlayer then
+        for _, item in ipairs(otherRanked) do
+            item.btn.Visible = false
+            setCmdRowSelected(item.btn, false)
+        end
+        otherRanked = {}
+    end
+
     if cmdFavHeader then
-        cmdFavHeader.Visible = not takesPlayer and #favRanked > 0
+        cmdFavHeader.Visible = not takesPlayer
         cmdFavHeader.LayoutOrder = 1
+        if type(updateFavHeader) == "function" then
+            updateFavHeader(#favRanked)
+        end
+    end
+    if cmdFavEmptyHint then
+        cmdFavEmptyHint.Visible = not takesPlayer and #favRanked == 0
+        cmdFavEmptyHint.LayoutOrder = 2
+        cmdFavEmptyHint.Text = T("favEmpty")
     end
     if cmdAllHeader then
-        cmdAllHeader.Visible = not takesPlayer and #favRanked > 0 and #otherRanked > 0
+        cmdAllHeader.Visible = not takesPlayer and not cmdFavOnlyFilter and #favRanked > 0 and #otherRanked > 0
         cmdAllHeader.LayoutOrder = 500
     end
 
@@ -10951,7 +11016,9 @@
         -- Keep the panel open until intro autohide finishes
     elseif not Ianim then
         if indexnum == 0 then
-            if not cmdbar then
+            if cmdFavEmptyHint and cmdFavEmptyHint.Visible then
+                maximizeHolder()
+            elseif not cmdbar then
                 minimizeHolder()
             elseif cmdbar then
                 cmdbarHolder()
