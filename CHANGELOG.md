# Changelog

## 7.63 — 2026-09-15

### Added
- `showhistory` (`hist`, `cmdhistory`) — lists recent commands from this session; empty history shows a clear notice instead of crashing.

### Fixed
- `lastcommand` / `lastcmd` no longer errors when history is empty.
