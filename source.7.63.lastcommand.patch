Apply onto main `source` before merge (1MB blob does not fit this connector).

HUNK 1 — version
- currentVersion = '7.50'
+ currentVersion = '7.63'

HUNK 2 — UI_L.en after noDesc
+ lastCmdEmpty = "Last command",
+ lastCmdEmptyText = "No previous command yet",

HUNK 3 — UI_L.ru after noDesc
+ lastCmdEmpty = "Последняя команда",
+ lastCmdEmptyText = "Пока нет предыдущей команды",

HUNK 4 — replace lastcommand addcmd body
addcmd('lastcommand',{'lastcmd'},function(args, speaker)
    local prev = cmdHistory and cmdHistory[1]
    if type(prev) ~= 'string' or prev == '' then
        notify(T('lastCmdEmpty'), T('lastCmdEmptyText'))
        return
    end
    local low = prev:lower()
    if low:sub(1,11) ~= 'lastcommand' and low:sub(1,7) ~= 'lastcmd' then
        execCmd(prev)
    else
        notify(T('lastCmdEmpty'), T('lastCmdEmptyText'))
    end
end)
