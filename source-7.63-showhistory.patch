--- source
+++ source
@@ -485,7 +485,7 @@
     end)
 end
 
-currentVersion = '7.50'
+currentVersion = '7.63'
 BRAND_NAME = 'Infinity Yield Plus'
 
 PANEL_W = 300
@@ -561,6 +561,8 @@
 		removeFavorite = "Remove from favorites",
 		favHintAdd = "★  Right-click to favorite",
 		favHintRemove = "★  Favorited · right-click to remove",
+		histTitle = "Command history",
+		histEmpty = "No saved commands yet",
 		refTitle = "Reference",
 		refSpecialHeader = "Special Player Cases",
 		refSpecialIntro = "These keywords can be used to quickly select groups of players in commands:",
@@ -786,6 +788,8 @@
 		removeFavorite = "Убрать из избранного",
 		favHintAdd = "★  ПКМ — в избранное",
 		favHintRemove = "★  В избранном · ПКМ — убрать",
+		histTitle = "История команд",
+		histEmpty = "Сохранённых команд пока нет",
 		refTitle = "Справка",
 		refSpecialHeader = "Особые цели игроков",
 		refSpecialIntro = "Эти ключевые слова быстро выбирают группы игроков в командах:",
@@ -11264,6 +11268,7 @@
 CMDs[#CMDs + 1] = {NAME = 'togglefullscreen / togglefs', DESC = 'Toggles fullscreen'}
 CMDs[#CMDs + 1] = {NAME = 'notify [text]', DESC = 'Sends you a notification with the provided text'}
 CMDs[#CMDs + 1] = {NAME = 'lastcommand / lastcmd', DESC = 'Executes the previous command used'}
+CMDs[#CMDs + 1] = {NAME = 'showhistory / hist / cmdhistory', DESC = 'Shows the last saved commands'}
 CMDs[#CMDs + 1] = {NAME = 'exit', DESC = 'Kills Roblox process (or go back to main menu)'}
 CMDs[#CMDs + 1] = {NAME = '', DESC = ''}
 CMDs[#CMDs + 1] = {NAME = 'antivoid / voidbooster / voidboost', DESC = 'Launches you up when You\'re close to the Void'}
@@ -12032,6 +12037,7 @@
 		killiy = 'Выгружает Infinity Yield Plus',
 		killiyp = 'Выгружает Infinity Yield Plus',
 		lastcommand = 'Описание: Executes the previous command used',
+		showhistory = 'Показывает последние сохранённые команды',
 		lay = 'Меняет твой/твою character lay down',
 		light = 'Даёт your игрока dynamic light',
 		locate = 'Показать a single игрока and their status',
@@ -12795,7 +12801,7 @@
                 if not speaker then speaker = Players.LocalPlayer end
                 if store then
                     if speaker == Players.LocalPlayer then
-                        if cmdHistory[1] ~= rawCmdStr and rawCmdStr:sub(1,11) ~= 'lastcommand' and rawCmdStr:sub(1,7) ~= 'lastcmd' then
+                        if cmdHistory[1] ~= rawCmdStr and rawCmdStr:sub(1,11) ~= 'lastcommand' and rawCmdStr:sub(1,7) ~= 'lastcmd' and rawCmdStr:sub(1,11) ~= 'showhistory' and rawCmdStr:sub(1,4) ~= 'hist' and rawCmdStr:sub(1,10) ~= 'cmdhistory' then
                             table.insert(cmdHistory,1,rawCmdStr)
                         end
                     end
@@ -18192,11 +18198,26 @@
 end)
 
 addcmd('lastcommand',{'lastcmd'},function(args, speaker)
-    if cmdHistory[1]:sub(1,11) ~= 'lastcommand' and cmdHistory[1]:sub(1,7) ~= 'lastcmd' then
+    if cmdHistory[1] and cmdHistory[1]:sub(1,11) ~= 'lastcommand' and cmdHistory[1]:sub(1,7) ~= 'lastcmd' then
         execCmd(cmdHistory[1])
     end
 end)
 
+addcmd('showhistory',{'hist','cmdhistory'},function(args, speaker)
+    local list = cmdHistory or {}
+    if type(list) ~= "table" or #list == 0 then
+        notify(T("histTitle"), T("histEmpty"))
+        return
+    end
+    local n = math.min(8, #list)
+    local lines = {}
+    for i = 1, n do
+        lines[#lines + 1] = tostring(i) .. ". " .. tostring(list[i])
+    end
+    notify(T("histTitle"), table.concat(lines, "\n"))
+end)
+registerRuDesc('showhistory', 'Показывает последние сохранённые команды')
+
 addcmd('windowfocus', {}, function(args, speaker)
     wfenabled = true
 end)
