--- a/source
+++ b/source
@@ -485,7 +485,7 @@
     end)
 end
 
-currentVersion = '7.50'
+currentVersion = '7.63'
 BRAND_NAME = 'Infinity Yield Plus'
 
 PANEL_W = 300
@@ -2039,40 +2039,65 @@
 visibleCmdButtons = {}
 playerSuggestButtons = {}
 
-function formatCmdRich(text)
+function escapeCmdHtml(text)
+	return tostring(text or ""):gsub("&", "&amp;"):gsub("<", "&lt;"):gsub(">", "&gt;")
+end
+
+function paintCmdMatch(plain, query, baseColor)
+	local raw = tostring(plain or "")
+	if raw == "" then
+		return ""
+	end
+	local q = tostring(query or "")
+	if q == "" then
+		return "<font color=\"" .. baseColor .. "\">" .. escapeCmdHtml(raw) .. "</font>"
+	end
+	local at = raw:lower():find(q:lower(), 1, true)
+	if not at then
+		return "<font color=\"" .. baseColor .. "\">" .. escapeCmdHtml(raw) .. "</font>"
+	end
+	local before = raw:sub(1, at - 1)
+	local mid = raw:sub(at, at + #q - 1)
+	local after = raw:sub(at + #q)
+	return "<font color=\"" .. baseColor .. "\">" .. escapeCmdHtml(before) .. "</font>"
+		.. "<font color=\"#FDE68A\">" .. escapeCmdHtml(mid) .. "</font>"
+		.. "<font color=\"" .. baseColor .. "\">" .. escapeCmdHtml(after) .. "</font>"
+end
+
+function formatCmdRich(text, query)
 	if not text or text == "" then
 		return ""
 	end
-	local escaped = tostring(text):gsub("&", "&amp;"):gsub("<", "&lt;"):gsub(">", "&gt;")
-	local argAt = escaped:find("%[")
-	local names = escaped
+	local raw = tostring(text)
+	local argAt = raw:find("%[", 1, true)
+	local names = raw
 	local args = ""
 	if argAt then
-		names = escaped:sub(1, argAt - 1):gsub("%s+$", "")
-		args = escaped:sub(argAt)
+		names = raw:sub(1, argAt - 1):gsub("%s+$", "")
+		args = raw:sub(argAt)
 	end
 	local primary, aliases = names:match("^([^/]+)%s*(.*)$")
 	primary = (primary or names):gsub("%s+$", "")
 	aliases = aliases or ""
-	local html = "<font color=\"#B8B4CC\">·</font> <font color=\"#F4F4F8\">" .. primary .. "</font>"
+	local html = "<font color=\"#B8B4CC\">·</font> " .. paintCmdMatch(primary, query, "#F4F4F8")
 	if aliases ~= "" then
-		html = html .. "<font color=\"#7A778C\"> " .. aliases .. "</font>"
+		html = html .. " " .. paintCmdMatch(aliases, query, "#7A778C")
 	end
 	if args ~= "" then
-		html = html .. " <font color=\"#C4B5FD\">" .. args .. "</font>"
+		html = html .. " " .. paintCmdMatch(args, query, "#C4B5FD")
 	end
 	return html
 end
 
-function applyCmdRowText(btn, text)
+function applyCmdRowText(btn, text, query)
 	if not btn then return end
 	btn.Text = text
 	local label = btn:FindFirstChild("Label")
 	if label then
 		if btn:GetAttribute("SuggestKind") == "player" then
-			label.Text = "<font color=\"#EDE9FE\">" .. tostring(text):gsub("&", "&amp;"):gsub("<", "&lt;") .. "</font>"
+			label.Text = paintCmdMatch(text, query, "#EDE9FE")
 		else
-			label.Text = formatCmdRich(text)
+			label.Text = formatCmdRich(text, query)
 		end
 	end
 end
@@ -10889,6 +10914,7 @@
         if type(applyFavoriteStyle) == "function" then
             applyFavoriteStyle(item.btn)
         end
+        applyCmdRowText(item.btn, item.btn.Text, str)
         visibleCmdButtons[#visibleCmdButtons + 1] = item.btn
         if topCommand == nil then
             topCommand = item.btn.Text
@@ -10918,7 +10944,7 @@
             if i > 24 then break end
             local btn = getPlayerSuggestButton(i)
             btn.Text = sug.label
-            applyCmdRowText(btn, sug.label)
+            applyCmdRowText(btn, sug.label, argQuery)
             btn:SetAttribute("Title", sug.label)
             btn:SetAttribute("Desc", sug.desc)
             btn:SetAttribute("SuggestKind", "player")
