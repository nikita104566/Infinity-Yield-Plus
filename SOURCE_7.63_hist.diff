diff --git a/source b/source
index 97e1997..5f74602 100644
--- a/source
+++ b/source
@@ -485,7 +485,7 @@ if makefolder and isfolder and writefile and isfile and readfile then
     end)
 end
 
-currentVersion = '7.50'
+currentVersion = '7.63'
 BRAND_NAME = 'Infinity Yield Plus'
 
 PANEL_W = 300
@@ -561,6 +561,7 @@ UI_L = {
 		removeFavorite = "Remove from favorites",
 		favHintAdd = "★  Right-click to favorite",
 		favHintRemove = "★  Favorited · right-click to remove",
+		histEmpty = "No command history yet",
 		refTitle = "Reference",
 		refSpecialHeader = "Special Player Cases",
 		refSpecialIntro = "These keywords can be used to quickly select groups of players in commands:",
@@ -786,6 +787,7 @@ UI_L = {
 		removeFavorite = "Убрать из избранного",
 		favHintAdd = "★  ПКМ — в избранное",
 		favHintRemove = "★  В избранном · ПКМ — убрать",
+		histEmpty = "История команд пока пуста",
 		refTitle = "Справка",
 		refSpecialHeader = "Особые цели игроков",
 		refSpecialIntro = "Эти ключевые слова быстро выбирают группы игроков в командах:",
@@ -8751,6 +8753,7 @@ function saves()
                     if vtype(json.WayPoints, "table") then AllWaypoints = json.WayPoints else WayPoints = {} AllWaypoints = {} end
                     if vtype(json.PluginsTable, "table") then PluginsTable = json.PluginsTable else PluginsTable = {} end
                     if vtype(json.favCmds, "table") then favCmds = json.favCmds else favCmds = {} end
+                    if vtype(json.cmdHistory, "table") then IYP_SAVED_HIST = json.cmdHistory else IYP_SAVED_HIST = {} end
                     if vtype(json.currentShade1, "table") then currentShade1 = Color3.new(json.currentShade1[1],json.currentShade1[2],json.currentShade1[3]) end
                     if vtype(json.currentShade2, "table") then currentShade2 = Color3.new(json.currentShade2[1],json.currentShade2[2],json.currentShade2[3]) end
                     if vtype(json.currentShade3, "table") then currentShade3 = Color3.new(json.currentShade3[1],json.currentShade3[2],json.currentShade3[3]) end
@@ -8838,6 +8841,7 @@ function updatesaves()
             WayPoints = AllWaypoints;
             PluginsTable = PluginsTable;
             favCmds = favCmds or {};
+            cmdHistory = cmdHistory or {};
             currentShade1 = {currentShade1.R,currentShade1.G,currentShade1.B};
             currentShade2 = {currentShade2.R,currentShade2.G,currentShade2.B};
             currentShade3 = {currentShade3.R,currentShade3.G,currentShade3.B};
@@ -12736,6 +12740,18 @@ function splitString(str,delim)
 end
 
 cmdHistory = {}
+if type(IYP_SAVED_HIST) == "table" and #IYP_SAVED_HIST > 0 then
+    local restored = {}
+    for i = 1, math.min(#IYP_SAVED_HIST, 30) do
+        local item = IYP_SAVED_HIST[i]
+        if type(item) == "string" and item ~= "" then
+            restored[#restored + 1] = item
+        end
+    end
+    if #restored > 0 then cmdHistory = restored end
+    IYP_SAVED_HIST = nil
+end
+IYP_HIST_SAVE_T = 0
 local lastCmds = {}
 local historyCount = 0
 local split=" "
@@ -12800,6 +12816,10 @@ function execCmd(cmdStr,speaker,store)
                         end
                     end
                     if #cmdHistory > 30 then table.remove(cmdHistory) end
+                    if type(updatesaves) == "function" and (tick() - (IYP_HIST_SAVE_T or 0)) > 0.35 then
+                        IYP_HIST_SAVE_T = tick()
+                        pcall(updatesaves)
+                    end
 
                     lastCmds[cmdName] = v
                 end
@@ -18192,8 +18212,13 @@ addcmd('notify',{},function(args, speaker)
 end)
 
 addcmd('lastcommand',{'lastcmd'},function(args, speaker)
-    if cmdHistory[1]:sub(1,11) ~= 'lastcommand' and cmdHistory[1]:sub(1,7) ~= 'lastcmd' then
-        execCmd(cmdHistory[1])
+    local last = cmdHistory and cmdHistory[1]
+    if type(last) ~= "string" or last == "" then
+        notify("History", T("histEmpty"))
+        return
+    end
+    if last:sub(1,11) ~= 'lastcommand' and last:sub(1,7) ~= 'lastcmd' then
+        execCmd(last)
     end
 end)
