Apply inside source (not a second loadstring).

1) currentVersion = '7.63'

2) In formatCmdRich, after computing primary/aliases, do NOT append aliases to html.
Keep:
  local html = "<font color=\"#B8B4CC\">·</font> <font color=\"#F4F4F8\">" .. primary .. "</font>"
  if args ~= "" then
    html = html .. " <font color=\"#C4B5FD\">" .. args .. "</font>"
  end

3) In showHelper, title = primary name only; if aliases ~= "" then desc = aliases .. newline .. desc.
