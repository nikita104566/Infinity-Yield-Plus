--- source
+++ source
@@ -485,7 +485,7 @@
     end)
 end
 
-currentVersion = '7.50'
+currentVersion = '7.63'
 BRAND_NAME = 'Infinity Yield Plus'
 
 PANEL_W = 300
@@ -561,6 +561,7 @@
 		removeFavorite = "Remove from favorites",
 		favHintAdd = "★  Right-click to favorite",
 		favHintRemove = "★  Favorited · right-click to remove",
+		cmdNoMatch = "No commands match",
 		refTitle = "Reference",
 		refSpecialHeader = "Special Player Cases",
 		refSpecialIntro = "These keywords can be used to quickly select groups of players in commands:",
@@ -786,6 +787,7 @@
 		removeFavorite = "Убрать из избранного",
 		favHintAdd = "★  ПКМ — в избранное",
 		favHintRemove = "★  В избранном · ПКМ — убрать",
+		cmdNoMatch = "Нет таких команд",
 		refTitle = "Справка",
 		refSpecialHeader = "Особые цели игроков",
 		refSpecialIntro = "Эти ключевые слова быстро выбирают группы игроков в командах:",
@@ -1182,6 +1184,7 @@
 	end
 	if cmdFavHeader then cmdFavHeader.Text = T("favorites") end
 	if cmdAllHeader then cmdAllHeader.Text = T("allCommands") end
+	if cmdEmpty then cmdEmpty.Text = T("cmdNoMatch") end
 	if PopupText_2 then PopupText_2.Text = T("keybindEditorTitle") end
 	if Directions then Directions.Text = T("keybindHint") end
 	if Cmdbar_2 then Cmdbar_2.PlaceholderText = T("keybindPickCmd") end
@@ -1751,6 +1754,24 @@
 cmdAllHeader.Visible = false
 cmdAllHeader.LayoutOrder = 500
 
+cmdEmpty = Instance.new("TextLabel")
+cmdEmpty.Name = "IYR_EMPTY"
+cmdEmpty.Parent = CMDsF
+cmdEmpty.BackgroundTransparency = 1
+cmdEmpty.BorderSizePixel = 0
+cmdEmpty.Size = UDim2.new(1, -(SCROLL_GUTTER + 2), 0, 28)
+cmdEmpty.Font = UI_FONT
+cmdEmpty.TextSize = 12
+cmdEmpty.TextXAlignment = Enum.TextXAlignment.Left
+cmdEmpty.TextYAlignment = Enum.TextYAlignment.Top
+cmdEmpty.TextColor3 = Color3.fromRGB(154, 148, 176)
+cmdEmpty.Text = T("cmdNoMatch")
+cmdEmpty.TextTruncate = Enum.TextTruncate.AtEnd
+cmdEmpty.TextWrapped = true
+cmdEmpty.ZIndex = 11
+cmdEmpty.Visible = false
+cmdEmpty.LayoutOrder = 2
+
 SettingsButton.Name = "SettingsButton"
 SettingsButton.Parent = Holder
 SettingsButton.BackgroundTransparency = 1
@@ -10881,6 +10902,12 @@
         cmdAllHeader.Visible = not takesPlayer and #favRanked > 0 and #otherRanked > 0
         cmdAllHeader.LayoutOrder = 500
     end
+    if cmdEmpty then
+        local typed = (str or ""):gsub("^%s+", ""):gsub("%s+$", "")
+        cmdEmpty.Visible = (#ranked == 0) and (typed ~= "") and (not takesPlayer)
+        cmdEmpty.LayoutOrder = 2
+        cmdEmpty.Text = T("cmdNoMatch")
+    end
 
     local function placeCmdRow(item, order)
         indexnum += 1
@@ -12264,8 +12291,34 @@
 	local title = tostring(btn:GetAttribute("Title") or btn.Text)
 	local fav = FavCmds.is(title)
 	local label = btn:FindFirstChild("Label")
+	local mark = btn:FindFirstChild("FavMark")
+	if fav and not mark then
+		mark = Instance.new("TextLabel")
+		mark.Name = "FavMark"
+		mark.BackgroundTransparency = 1
+		mark.Size = UDim2.new(0, 12, 1, 0)
+		mark.Position = UDim2.new(0, 0, 0, 0)
+		mark.Font = UI_FONT
+		mark.TextSize = 12
+		mark.TextXAlignment = Enum.TextXAlignment.Left
+		mark.TextYAlignment = Enum.TextYAlignment.Center
+		mark.TextColor3 = Color3.fromRGB(255, 214, 102)
+		mark.Text = "*"
+		mark.ZIndex = 12
+		mark.Parent = btn
+	end
+	if mark then
+		mark.Visible = fav and true or false
+	end
 	if label then
 		label.TextColor3 = fav and Color3.fromRGB(255, 214, 102) or Color3.fromRGB(244, 244, 248)
+		if fav then
+			label.Position = UDim2.new(0, 12, 0, 0)
+			label.Size = UDim2.new(1, -12, 1, 0)
+		else
+			label.Position = UDim2.new(0, 0, 0, 0)
+			label.Size = UDim2.new(1, 0, 1, 0)
+		end
 	end
 end
 function FavCmds.find(key)
