--- source
+++ source
@@ -485,7 +485,7 @@
     end)
 end
 
-currentVersion = '7.50'
+currentVersion = '7.63'
 BRAND_NAME = 'Infinity Yield Plus'
 
 PANEL_W = 300
@@ -561,6 +561,7 @@
 		removeFavorite = "Remove from favorites",
 		favHintAdd = "★  Right-click to favorite",
 		favHintRemove = "★  Favorited · right-click to remove",
+		favEmptyHint = "Star a command to pin it here",
 		refTitle = "Reference",
 		refSpecialHeader = "Special Player Cases",
 		refSpecialIntro = "These keywords can be used to quickly select groups of players in commands:",
@@ -786,6 +787,7 @@
 		removeFavorite = "Убрать из избранного",
 		favHintAdd = "★  ПКМ — в избранное",
 		favHintRemove = "★  В избранном · ПКМ — убрать",
+		favEmptyHint = "Нажми звезду на команде, чтобы закрепить её здесь",
 		refTitle = "Справка",
 		refSpecialHeader = "Особые цели игроков",
 		refSpecialIntro = "Эти ключевые слова быстро выбирают группы игроков в командах:",
@@ -1181,6 +1183,7 @@
 		refreshKbCmdPicker()
 	end
 	if cmdFavHeader then cmdFavHeader.Text = T("favorites") end
+	if cmdFavEmpty then cmdFavEmpty.Text = T("favEmptyHint") end
 	if cmdAllHeader then cmdAllHeader.Text = T("allCommands") end
 	if PopupText_2 then PopupText_2.Text = T("keybindEditorTitle") end
 	if Directions then Directions.Text = T("keybindHint") end
@@ -1734,6 +1737,23 @@
 cmdFavHeader.Visible = false
 cmdFavHeader.LayoutOrder = 1
 
+cmdFavEmpty = Instance.new("TextLabel")
+cmdFavEmpty.Name = "IYR_FAV_EMPTY"
+cmdFavEmpty.Parent = CMDsF
+cmdFavEmpty.BackgroundTransparency = 1
+cmdFavEmpty.BorderSizePixel = 0
+cmdFavEmpty.Size = UDim2.new(1, -(SCROLL_GUTTER + 2), 0, 16)
+cmdFavEmpty.Font = UI_FONT
+cmdFavEmpty.TextSize = 11
+cmdFavEmpty.TextXAlignment = Enum.TextXAlignment.Left
+cmdFavEmpty.TextYAlignment = Enum.TextYAlignment.Center
+cmdFavEmpty.TextColor3 = Color3.fromRGB(132, 126, 154)
+cmdFavEmpty.Text = T("favEmptyHint")
+cmdFavEmpty.TextTruncate = Enum.TextTruncate.AtEnd
+cmdFavEmpty.ZIndex = 11
+cmdFavEmpty.Visible = false
+cmdFavEmpty.LayoutOrder = 2
+
 cmdAllHeader = Instance.new("TextLabel")
 cmdAllHeader.Name = "IYR_ALL_HDR"
 cmdAllHeader.Parent = CMDsF
@@ -10873,12 +10893,18 @@
         end)
     end
 
+    local showFavEmpty = (not takesPlayer) and #favRanked == 0 and (cmdQuery == nil or cmdQuery == "")
     if cmdFavHeader then
-        cmdFavHeader.Visible = not takesPlayer and #favRanked > 0
+        cmdFavHeader.Visible = (not takesPlayer) and (#favRanked > 0 or showFavEmpty)
         cmdFavHeader.LayoutOrder = 1
     end
+    if cmdFavEmpty then
+        cmdFavEmpty.Text = T("favEmptyHint")
+        cmdFavEmpty.Visible = showFavEmpty
+        cmdFavEmpty.LayoutOrder = 2
+    end
     if cmdAllHeader then
-        cmdAllHeader.Visible = not takesPlayer and #favRanked > 0 and #otherRanked > 0
+        cmdAllHeader.Visible = not takesPlayer and (#favRanked > 0 or showFavEmpty) and #otherRanked > 0
         cmdAllHeader.LayoutOrder = 500
     end
